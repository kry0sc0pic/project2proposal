{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "rm -rf flutter && git clone https://github.com/flutter/flutter.git flutter && cd flutter && git checkout 3.29.0 && cd .. && flutter/bin/flutter config --enable-web && flutter/bin/flutter pub get",
  "buildCommand": "flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
